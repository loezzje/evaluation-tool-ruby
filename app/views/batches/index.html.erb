<h1>Batches</h1>

<% @batches.each do |batch| %>
<p><%= link_to ("Batch ##{batch.name}"), batch_path(batch.id) %></p>
<% end %>

<button type="button" onclick="window.location.href='/batches/new'">Add batch</button>
